apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: reservation-app
stringData:
  # Keys that MySQL deployment expects
  root-password: "1010"
  database: "reservation_db"  # Add your database name here
  
  # Keep your existing keys for backend compatibility
  DB_PASSWORD: "1010"
  MYSQL_ROOT_PASSWORD: "1010"
  MYSQL_PASSWORD: "1010"